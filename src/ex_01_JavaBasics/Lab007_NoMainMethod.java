package ex_01_JavaBasics;

public class Lab007_NoMainMethod {




}
// Is this a valid?
/*Yes, the file is valid as a Java file even without the main method.
However, but it cannot be executed independently because main starts the program.
The JVM recognizes only the main method as the starting point, so without it the program cannot run independently
*/
//no main method
// The class is valid but cannot be run directly.